[user]
  name = Tyler Brazier
  email = tyler@tylerbrazier.com

[core]
  editor = vim

[diff]
  tool = vimdiff

[alias]
  a = add -A
  b = branch
  c = commit -v
  co = checkout
  d = difftool -y
  f = fetch --all --prune
  l = log --stat
  m = merge
  p = push
  r = remote
  s = status
  sd = stash drop
  sl = stash list
  ss = stash save
  out = "!f() { git branch -D $1; git push --delete origin $1; }; f"
  up = "!f() { git push origin $(git symbolic-ref -q --short HEAD); }; f"
